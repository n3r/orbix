/// OrbixKit — API client, auth, and media helpers for the Orbix tvOS app.
/// See `DTOs.swift` (Codable models mirroring the SP1 API), `OrbixClient.swift`
/// (the actor-based HTTP client), `TokenStore.swift` (Keychain-backed device
/// token storage), and `ImageLoader.swift` (cached poster/backdrop fetch).
public enum OrbixKit {
    public static let version = "0.1.0"
}
