NODE_ENV=development
DATABASE_URL=postgresql://orbix:orbix@localhost:1062/orbix
REDIS_URL=redis://localhost:1063
API_PORT=1061
WEB_PORT=1060
# REQUIRED: 32+ char random string. Generate with: openssl rand -base64 32
SESSION_SECRET=change_me
WEB_ORIGIN=http://localhost:1060
# METADATA_DIR=./data/metadata  # cached posters/backdrops; gitignored under /data/
# TRANSCODE_DIR=./data/transcode
# MODELS_DIR=./data/models       # local embedding model files; gitignored under /data/
# EMBEDDINGS_ENABLED=true        # set to false to disable embedding generation
# MAX_TRANSCODE_SESSIONS=4       # cap on concurrent ffmpeg transcode sessions (LRU-evicted past the cap)
